---
title: 'Kitchen Sink'
description: 'A comprehensive showcase of all available MDX directives and components in the NoMercy documentation system.'
sections:
  - title: 'Hero Pattern'
    id: 'hero-pattern'
  - title: 'Icon Cards'
    id: 'icon-cards'
  - title: 'Logo Cards'
    id: 'logo-cards'
  - title: 'Callouts'
    id: 'callouts'
  - title: 'Buttons'
    id: 'buttons'
  - title: 'Code Blocks'
    id: 'code-blocks'
  - title: 'Code Groups'
    id: 'code-groups'
  - title: 'Properties'
    id: 'properties'
  - title: 'Layout Columns'
    id: 'layout-columns'
  - title: 'Typography'
    id: 'typography'
---

::Hero

# Kitchen Sink

This page demonstrates every available component and directive in the NoMercy documentation system. Use this as a reference for building documentation pages. {{ className: 'lead' }}

---

## Hero Pattern {{ id: 'hero-pattern' }}

The hero pattern provides a decorative background element for landing pages. It's already applied to this page.

**Usage:**

```markdown
::Hero
```

---

## Icon Cards {{ id: 'icon-cards' }}

Icon cards display a grid of linked cards with icons, perfect for documentation sections.

::IconCards {{ heading: 'Documentation Sections' }}

::IconCard {{ href: '/app', name: 'App Documentation', icon: 'app' }}

Complete guide to setting up and using the NoMercy application.

::IconCard {{ href: '/mediaserver', name: 'Media Server', icon: 'server' }}

Configure and manage your media server instance.

::IconCard {{ href: '/quickstart', name: 'API Reference', icon: 'api' }}

Integrate with the NoMercy API programmatically.

::

**Usage:**

```markdown
::IconCards {{ heading: 'Documentation Sections' }}

::IconCard {{ href: '/path', name: 'Card Title', icon: 'api' }}

Card description goes here.

::IconCard {{ href: '/path', name: 'Another Card', icon: 'server' }}

Another description.

::
```

---

## Logo Cards {{ id: 'logo-cards' }}

Logo cards display a grid of items with custom logos, ideal for SDK libraries or integrations.

::LogoCards {{ heading: 'Supported Languages' }}

::LogoCard {{ name: 'JavaScript', href: '#', logo: '/images/logos/node.svg' }}

Build applications with Node.js and modern JavaScript frameworks.

::LogoCard {{ name: 'Python', href: '#', logo: '/images/logos/python.svg' }}

Integrate with Python for data processing and automation.

::LogoCard {{ name: 'PHP', href: '#', logo: '/images/logos/php.svg' }}

Server-side integration with PHP applications.

::

**Usage:**

```markdown
::LogoCards {{ heading: 'Official SDKs' }}

::LogoCard {{ name: 'Node.js', href: '#', logo: '/images/logos/node.svg' }}

Description of the SDK or integration.

::
```

---

## Callouts {{ id: 'callouts' }}

Callouts highlight important information with an info icon and colored background.

::Callout

This is a callout component. Use it to draw attention to important notes, warnings, or tips that users should be aware of before proceeding.

::

**Usage:**

```markdown
::Callout

Your important message here. Can span multiple paragraphs.

::
```

---

## Buttons {{ id: 'buttons' }}

Buttons can be styled with different variants and can include icons.

### Text Variant

::button {{ url: '/quickstart', variant: 'text' }}

Get Started

::icon arrow-right

::

### Outline Variant

::button {{ url: '/sdks', variant: 'outline' }}

View SDKs

::icon arrow-right

::

### Multiple Buttons in a Row

<div className="not-prose mt-6 mb-16 flex gap-3">

::button {{ url: '/app', variant: 'outline' }}

App Setup

::icon arrow-right

::

::button {{ url: '/mediaserver', variant: 'outline' }}

Media Server

::icon arrow-right

::

::button {{ url: '/quickstart', variant: 'outline' }}

API Quickstart

::icon arrow-right

::

</div>

**Usage:**

```markdown
::button {{ url: '/path', variant: 'text' }}

Button Label

::icon arrow-right

::
```

---

## Code Blocks {{ id: 'code-blocks' }}

Single code blocks with syntax highlighting and optional titles.

```bash {{ title: 'Install dependencies' }}
npm install @nomercy/sdk
```

```javascript {{ title: 'Initialize the client' }}
import { NoMercyClient } from '@nomercy/sdk';

const client = new NoMercyClient({
  apiKey: process.env.NOMERCY_API_KEY,
});
```

```python {{ title: 'Python example' }}
from nomercy import Client

client = Client(api_key='your-api-key')
response = client.media.list()
```

**Usage:**

````markdown
```javascript {{ title: 'Example code' }}
const example = 'code here';
```
````

---

## Code Groups {{ id: 'code-groups' }}

Code groups display multiple language examples with tabs.

::code-group

```bash {{ title: 'cURL' }}
curl -X GET https://api.nomercy.tv/v1/media \
  -H "Authorization: Bearer {token}"
```

```javascript
import { NoMercyClient } from '@nomercy/sdk';

const client = new NoMercyClient({ apiKey: token });
const media = await client.media.list();
```

```python
from nomercy import Client

client = Client(api_key=token)
media = client.media.list()
```

```php
$client = new NoMercy\Client($token);
$media = $client->media->list();
```

::

**Usage:**

````markdown
::code-group

```bash {{ title: 'cURL' }}
curl example
```

```javascript
// JS example
```

::
````

---

## Properties {{ id: 'properties' }}

Properties display API parameters, fields, or configuration options.

::properties

::property {{ name: 'api_key', type: 'string' }}

Your API authentication key. Required for all requests.

::property {{ name: 'timeout', type: 'integer' }}

Request timeout in milliseconds. Defaults to 30000.

::property {{ name: 'retries', type: 'integer' }}

Number of retry attempts for failed requests. Defaults to 3.

::property {{ name: 'debug', type: 'boolean' }}

Enable debug logging for troubleshooting. Defaults to false.

::

**Usage:**

```markdown
::properties

::property {{ name: 'field_name', type: 'string' }}

Description of the property.

::
```

---

## Layout Columns {{ id: 'layout-columns' }}

Create multi-column layouts for side-by-side content.

::row

::col

### Left Column

This content appears in the left column. Perfect for explanatory text, documentation, or instructions.

You can include:

- Paragraphs
- Lists
- Links
- Any markdown content

::col

```json {{ title: 'Example Response' }}
{
  "id": "12345",
  "name": "Example",
  "status": "active",
  "created_at": "2024-01-20T12:00:00Z"
}
```

::

### Sticky Column

::row

::col

Regular content in the left column that scrolls normally with the page.

::col sticky

```javascript
// This code block stays visible
// while you scroll the left column
const stickyExample = true;
```

::

**Usage:**

```markdown
::row

::col

Left column content

::col

Right column content

::

# Or with sticky:

::row

::col

Scrolling content

::col sticky

Fixed content

::
```

---

## Typography {{ id: 'typography' }}

### Headings

Standard markdown headings are automatically styled with anchor links.

# Heading 1

## Heading 2

### Heading 3

#### Heading 4

### Paragraphs

Regular paragraph text with **bold**, _italic_, and `inline code` formatting.

You can also use the lead class for emphasis:

This is a lead paragraph with larger text that stands out from regular content. {{ className: 'lead' }}

### Lists

**Unordered lists:**

- First item
- Second item
  - Nested item
  - Another nested item
- Third item

**Ordered lists:**

1. First step
2. Second step
3. Third step

### Links

Standard markdown links: [Visit NoMercy](/)

### Blockquotes

> This is a blockquote. Use it for important callouts or quotations.
> It can span multiple lines.

### Horizontal Rules

Use `---` for section dividers:

---

### Tables

| Feature     | Status | Notes           |
| ----------- | ------ | --------------- |
| Icon Cards  | ✓      | Fully supported |
| Logo Cards  | ✓      | Fully supported |
| Callouts    | ✓      | Fully supported |
| Code Groups | ✓      | Fully supported |

---

## Combinations

You can combine multiple components for complex layouts:

::row

::col

### Documentation + Code

Here's a common pattern combining explanatory text with a code example.

::Callout

Remember to replace `{token}` with your actual API key.

::

::col

```bash {{ title: 'Make a request' }}
curl https://api.nomercy.tv/v1/status \
  -H "Authorization: Bearer {token}"
```

::

---

## Summary

This page demonstrates all available directives:

- `::Hero` - Decorative background pattern
- `::IconCards` / `::IconCard` - Documentation section cards
- `::LogoCards` / `::LogoCard` - SDK/integration cards
- `::Callout` - Important notes and warnings
- `::button` with `::icon` - Call-to-action buttons
- `::code-group` - Multi-language code examples
- `::properties` / `::property` - API parameter documentation
- `::row` / `::col` / `::col sticky` - Multi-column layouts

All directives use the `::` prefix and avoid JSX imports, keeping your markdown clean and portable.
