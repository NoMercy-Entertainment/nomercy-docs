---
title: Core player methods
description: Every method the core surface answers for, from ecosystem contract 2.0.3.
---

The core player answers for 133 methods. Each is the same name,
the same arguments and the same return on Kotlin and Swift as on the web,
because all three are generated from one contract and checked against it.

Kotlin uses the bare-noun getter and setter pair the web contract defines:
`time()` reads and `time(seconds)` writes. Swift sees the same two through
Objective-C interop with the argument label spelled out.

### `activityTracking`

{/* partial */}
```ts
{
    (): boolean;
    (enabled: boolean): void;
  }
```

### `addClasses`

{/* partial */}
```ts
IPlayer<MusicEventMap>['addClasses']
```

### `addPlugin`

{/* partial */}
```ts
<P extends Plugin<any, any, any>>(PluginClass: PluginCtorWithId & (new () => P), opts?: P['opts']) => this
```

### `addTranslations`

{/* partial */}
```ts
(bundle: Translations) => void
```

### `announce`

{/* partial */}
```ts
(text: string, level?: AriaLiveLevel) => void
```

### `audioContext`

{/* partial */}
```ts
() => AudioContext | undefined
```

### `audioOutput`

{/* partial */}
```ts
{
    (): Promise<string | null>;
    (deviceId: string): Promise<void>;
  }
```

### `audioOutputs`

{/* partial */}
```ts
() => Promise<MediaDeviceInfo[]>
```

### `audioTrack`

{/* partial */}
```ts
{
    (): CurrentAudioTrackSelection | null;
    (idx: number): Promise<void>;
  }
```

### `audioTrackMode`

{/* partial */}
```ts
{
    (): AudioTrackState;
    (idx: number): void;
  }
```

### `audioTracks`

{/* partial */}
```ts
() => AudioTrack[]
```

### `auth`

{/* partial */}
```ts
{
    (): Readonly<AuthConfig> | undefined;
    (config: AuthConfig): void;
    (partial: Partial<AuthConfig>): void;
    (clear: null): void;
  }
```

### `backend`

{/* partial */}
```ts
(kind?: AudioBackendKind) => IAudioBackend | Promise<void>
```

### `backlog`

{/* partial */}
```ts
{
    (): ReadonlyArray<T>;
    (items: T[]): void;
  }
```

### `backlogAppend`

{/* partial */}
```ts
(item: T | T[]) => void
```

### `backlogClear`

{/* partial */}
```ts
() => void
```

### `backlogRemove`

{/* partial */}
```ts
(id: string | number) => void
```

### `bandwidth`

{/* partial */}
```ts
() => number
```

### `bandwidthEstimator`

{/* partial */}
```ts
{
    (): (() => number) | undefined;
    (fn: () => number): void;
  }
```

### `baseUrl`

{/* partial */}
```ts
{
    (): string | undefined;
    (url: string): void;
  }
```

### `bufferState`

{/* partial */}
```ts
() => BufferState
```

### `buffered`

{/* partial */}
```ts
() => number
```

### `bufferedRanges`

{/* partial */}
```ts
() => TimeRanges
```

### `bumpActivity`

{/* partial */}
```ts
() => void
```

### `canPlay`

{/* partial */}
```ts
(profile: { contentType: string; width?: number; height?: number; bitrate?: number; framerate?: number }) => Promise<CanPlayResult>
```

### `castState`

{/* partial */}
```ts
() => CastState
```

### `chapter`

{/* partial */}
```ts
{
    (): Chapter | null;
    (idx: number): void;
  }
```

### `chapters`

{/* partial */}
```ts
() => Chapter[]
```

### `container`

{/* partial */}
```ts
HTMLElement
```

### `createButton`

{/* partial */}
```ts
IPlayer<MusicEventMap>['createButton']
```

### `createElement`

{/* partial */}
```ts
IPlayer<MusicEventMap>['createElement']
```

### `createSVG`

{/* partial */}
```ts
IPlayer<MusicEventMap>['createSVG']
```

### `device`

{/* partial */}
```ts
() => DeviceCapabilities
```

### `dispatching`

{/* partial */}
```ts
() => ReadonlyArray<string>
```

### `dispose`

{/* partial */}
```ts
() => Promise<void>
```

### `duration`

{/* partial */}
```ts
() => number
```

### `enabledPlugins`

{/* partial */}
```ts
() => ReadonlyArray<Plugin>
```

### `experimental`

{/* partial */}
```ts
PlayerExperimental
```

### `forward`

{/* partial */}
```ts
(seconds?: number, opts?: ActionOptions) => Promise<void>
```

### `getPlugin`

{/* partial */}
```ts
<P extends object>(PluginClass: PluginCtorWithId & (new () => P)) => P | undefined
```

### `getPluginById`

{/* partial */}
```ts
<P extends object = object>(id: string) => P | undefined
```

### `getStreamFactory`

{/* partial */}
```ts
(id: string) => IStreamFactory | undefined
```

### `index`

{/* partial */}
```ts
() => number
```

### `isDesktop`

{/* partial */}
```ts
() => boolean
```

### `isMobile`

{/* partial */}
```ts
() => boolean
```

### `isTv`

{/* partial */}
```ts
() => boolean
```

### `item`

{/* partial */}
```ts
{
    (): T | undefined;
    (target: T | string | number, opts?: LoadOptions): void;
  }
```

### `language`

{/* partial */}
```ts
{
    (): string;
    (lang: string): Promise<void>;
  }
```

### `load`

{/* partial */}
```ts
(item: T, opts?: LoadOptions) => Promise<void>
```

### `loadQueue`

{/* partial */}
```ts
(url: string, parser?: (raw: string) => T[]) => Promise<void>
```

### `metrics`

{/* partial */}
```ts
() => PlaybackMetrics
```

### `mute`

{/* partial */}
```ts
() => Promise<void>
```

### `networkState`

{/* partial */}
```ts
() => NetworkState
```

### `next`

{/* partial */}
```ts
(opts?: LoadOptions) => Promise<void>
```

### `nextChapter`

{/* partial */}
```ts
(opts?: ActionOptions) => void
```

### `now`

{/* partial */}
```ts
() => number
```

### `options`

{/* partial */}
```ts
MusicPlayerConfig<T>
```

### `pause`

{/* partial */}
```ts
(opts?: ActionOptions) => Promise<void>
```

### `peekNext`

{/* partial */}
```ts
() => T | undefined
```

### `peekPrevious`

{/* partial */}
```ts
() => T | undefined
```

### `phase`

{/* partial */}
```ts
() => PlayerPhase
```

### `platform`

{/* partial */}
```ts
() => IPlatform
```

### `play`

{/* partial */}
```ts
(opts?: ActionOptions) => Promise<void>
```

### `playItem`

{/* partial */}
```ts
(
    target: BasePlaylistItem | string | number | ((item: BasePlaylistItem) => boolean),
    opts?: LoadOptions,
  ) => void
```

### `playNow`

{/* partial */}
```ts
(
    items: BasePlaylistItem[],
    start?: BasePlaylistItem | string | number | ((item: BasePlaylistItem) => boolean),
    opts?: LoadOptions,
  ) => void
```

### `playState`

{/* partial */}
```ts
() => PlayState
```

### `playbackRate`

{/* partial */}
```ts
{
    (): number;
    (rate: number): Promise<void>;
  }
```

### `playbackRates`

{/* partial */}
```ts
() => number[]
```

### `playerId`

{/* partial */}
```ts
string
```

### `plugins`

{/* partial */}
```ts
() => ReadonlyArray<Plugin>
```

### `preloadStrategy`

{/* partial */}
```ts
() => IPreloadStrategy
```

### `previous`

{/* partial */}
```ts
(opts?: LoadOptions) => Promise<void>
```

### `previousChapter`

{/* partial */}
```ts
(opts?: ActionOptions) => void
```

### `quality`

{/* partial */}
```ts
{
    (): CurrentQualitySelection | 'auto';
    (idx: number | 'auto'): void;
  }
```

### `qualityLevels`

{/* partial */}
```ts
{
    (): QualityLevel[];
    (opts: { includeUnsupported: true }): QualityLevel[];
  }
```

### `qualityMode`

{/* partial */}
```ts
{
    (): QualityState;
    (target: number | 'auto'): void;
  }
```

### `queue`

{/* partial */}
```ts
{
    (): ReadonlyArray<T>;
    (items: T[], opts?: ActionOptions): void;
  }
```

### `queueAppend`

{/* partial */}
```ts
(item: T | T[], opts?: ActionOptions) => void
```

### `queueClear`

{/* partial */}
```ts
(opts?: ActionOptions) => void
```

### `queueIndexOf`

{/* partial */}
```ts
(id: string | number) => number
```

### `queueInsert`

{/* partial */}
```ts
(item: T | T[], index: number, opts?: ActionOptions) => void
```

### `queueLength`

{/* partial */}
```ts
() => number
```

### `queueMove`

{/* partial */}
```ts
(from: number, to: number, opts?: ActionOptions) => void
```

### `queuePrepend`

{/* partial */}
```ts
(item: T | T[], opts?: ActionOptions) => void
```

### `queueRemove`

{/* partial */}
```ts
(id: string | number, opts?: ActionOptions) => void
```

### `queueRemoveAt`

{/* partial */}
```ts
(index: number, opts?: ActionOptions) => void
```

### `queueShuffle`

{/* partial */}
```ts
(opts?: ActionOptions) => void
```

### `queueSort`

{/* partial */}
```ts
(compare: (itemA: T, itemB: T) => number, opts?: ActionOptions) => void
```

### `ready`

{/* partial */}
```ts
() => Promise<void>
```

### `recordMetric`

{/* partial */}
```ts
(name: string, value: number) => void
```

### `refreshAuth`

{/* partial */}
```ts
() => Promise<void>
```

### `registerCueParser`

{/* partial */}
```ts
(parser: ICueParser, prepend?: boolean) => void
```

### `registerStream`

{/* partial */}
```ts
(factory: IStreamFactory, prepend?: boolean) => this
```

### `registerTitleTokens`

{/* partial */}
```ts
(tokens: Record<string, string>) => void
```

### `removeClasses`

{/* partial */}
```ts
IPlayer<MusicEventMap>['removeClasses']
```

### `removePlugin`

{/* partial */}
```ts
<P extends Plugin<any, any, any>>(PluginClass: PluginCtorWithId & (new () => P)) => void
```

### `removePluginById`

{/* partial */}
```ts
(id: string) => void
```

### `removeTranslations`

{/* partial */}
```ts
(prefix: string, lang?: string) => void
```

### `repeatState`

{/* partial */}
```ts
{
    (): RepeatState;
    (state: RepeatState): Promise<void>;
  }
```

### `resolveCueParser`

{/* partial */}
```ts
(url: string) => ICueParser | undefined
```

### `resolveUrl`

{/* partial */}
```ts
(url: string, category?: UrlCategory) => Promise<ResolvedUrl>
```

### `restart`

{/* partial */}
```ts
(opts?: ActionOptions) => Promise<void>
```

### `rewind`

{/* partial */}
```ts
(seconds?: number, opts?: ActionOptions) => Promise<void>
```

### `seekByPercentage`

{/* partial */}
```ts
(pct: number, opts?: ActionOptions) => void
```

### `seekToChapter`

{/* partial */}
```ts
(idx: number, opts?: ActionOptions) => void
```

### `seekToIndex`

{/* partial */}
```ts
(position: number, opts?: ActionOptions) => void
```

### `seekable`

{/* partial */}
```ts
() => TimeRanges
```

### `selectAudioOutput`

{/* partial */}
```ts
() => Promise<MediaDeviceInfo | null>
```

### `setPreloadStrategy`

{/* partial */}
```ts
(strategy: IPreloadStrategy) => void
```

### `setTransitionStrategy`

{/* partial */}
```ts
(strategy: ITransitionStrategy) => void
```

### `setup`

{/* partial */}
```ts
(config: MusicPlayerConfig<T>) => this
```

### `setupState`

{/* partial */}
```ts
() => SetupState
```

### `shuffleState`

{/* partial */}
```ts
{
    (): ShuffleState;
    (state: ShuffleState | boolean): Promise<void>;
  }
```

### `stop`

{/* partial */}
```ts
(opts?: ActionOptions) => Promise<void>
```

### `streamState`

{/* partial */}
```ts
() => string
```

### `streams`

{/* partial */}
```ts
() => ReadonlyArray<string>
```

### `t`

{/* partial */}
```ts
{
    (key: string, vars?: Record<string, string>): string;
    (PluginClass: PluginCtorWithId, key: string, vars?: Record<string, string>): string;
  }
```

### `time`

{/* partial */}
```ts
{
    (): number;
    (seconds: number, opts?: ActionOptions): Promise<void>;
  }
```

### `timeData`

{/* partial */}
```ts
() => KitTimeState
```

### `toggleMute`

{/* partial */}
```ts
() => void
```

### `togglePlayback`

{/* partial */}
```ts
(opts?: ActionOptions) => Promise<void>
```

### `transferTo`

{/* partial */}
```ts
(target: CastTarget) => Promise<void>
```

### `transitionStrategy`

{/* partial */}
```ts
() => ITransitionStrategy
```

### `translation`

{/* partial */}
```ts
{
    (lang: string, key: string): string | undefined;
    (lang: string, key: string, value: string): void;
  }
```

### `unmute`

{/* partial */}
```ts
() => Promise<void>
```

### `unregisterCueParser`

{/* partial */}
```ts
(id: string) => void
```

### `unregisterStream`

{/* partial */}
```ts
(id: string) => this
```

### `urlResolver`

{/* partial */}
```ts
{
    (): IUrlResolver | undefined;
    (resolver: IUrlResolver | undefined): void;
  }
```

### `visibilityState`

{/* partial */}
```ts
() => VisibilityState
```

### `volume`

{/* partial */}
```ts
{
    (): number;
    (level: number): Promise<void>;
  }
```

### `volumeDown`

{/* partial */}
```ts
(step?: number) => void
```

### `volumeState`

{/* partial */}
```ts
() => VolumeState
```

### `volumeUp`

{/* partial */}
```ts
(step?: number) => void
```
