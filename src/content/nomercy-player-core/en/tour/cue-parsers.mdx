---
title: 'Cue Parsers'
description: 'CueParserRegistry and the built-in VTT, sprite-VTT, and LRC parsers, plus CueList: the time-indexed, binary-searched structure every parser returns.'
---

{/* Source: packages/nomercy-player-core/src/adapters/cue-parser/registry.ts, src/adapters/cue-parser/built-ins.ts, src/core/cues/cue.ts */}

# Core: Cue Parsers

Subtitles, lyrics, and sprite-preview thumbnails are all the same shape underneath: timed text or data, looked up by playback position. The kit parses all three into one structure.

## CueList

`createCueList(cues)` builds an immutable, time-indexed `CueList<T>` from `{ start, end, payload }` entries. Input order does not matter, since it sorts by `start` on construction. Each query on the list does one job:

| Query | Returns |
| --- | --- |
| `active(time)` | Every cue whose interval contains `time`. Overlapping cues are supported on purpose, for parallel subtitle tracks and adjacent sprite thumbnails. |
| `next(time)` | The nearest cue after `time`. |
| `prev(time)` | The nearest cue before `time`. |

Every lookup is a binary search.

`createMutableCueList()` gives the same query surface over a live buffer that grows and shrinks at runtime, for streamed-in captions or chapter lists. It notifies `subscribe()`d listeners on every change.

## Built-in parsers

Three `ICueParser` implementations ship with the kit and auto-register during `setup()`:

- `lrcParser`: `.lrc`, plain or word-level enhanced LRC
- `vttSubtitleParser`: `.vtt`, WebVTT with inline `<b>` / `<i>` / `<u>` tags preserved and cue-setting alignment
- `spriteVttParser`: sprite-sheet crops via `#xywh=` fragments, only for URLs that hint at sprites

Each has an `id`, a `canParse(url, contentType?)` predicate, and a `parse(raw, opts?)` function.

## The registry

`CueParserRegistry` resolves most-recently-registered first.

`register(parser)` adds one, or replaces an existing entry with the same `id`. `resolve(url, contentType?)` walks the list newest-first and returns the first match.

A real player seeds the registry with the three built-ins during `setup()`. So a parser you register afterwards, or pass as `setup({ cueParsers: [...] })`, wins for the same URL pattern without disabling the built-in.

The runtime wrappers around the same registry are `player.registerCueParser(parser, prepend?)`, `unregisterCueParser(id)` and `resolveCueParser(url)`.

The example below parses real VTT and LRC text directly. `parseVtt`, `parseVttSubtitles`, `parseLrc`, and their sprite counterpart are exported standalone, no player needed. The example then registers a small custom format against a bare registry, to show the extension point in isolation.

:::snippet{file="tour-cue-parsers" live="false"}
:::

## Next steps

- [Typed Errors](/nomercy-player-core/tour/errors): the `PlayerError` hierarchy. Every failure path in this tour surfaces through it, from a rejected `ready()` to a parse failure.
