---
title: storage.temp
description: scratch space the host purges
tags: [capability, storage-temp]
---

{/* Generated from server/plugin-contract/contract/capabilities.json. Do not edit. */}

# `storage.temp`

Scratch space the host purges.

## What it covers

| | |
| --- | --- |
| Facade | `IPluginContext.Storage.Temp` |
| Scope | quota |
| Trust | low |
| Revocable while the plugin runs | yes |

The owner grants this without being asked a second time.

Taking this grant away stops the next call. The plugin keeps running.

## Asking for it

Name it in the manifest:

```json
{
  "capabilities": [
    { "name": "storage.temp", "scope": "*" }
  ]
}
```

The scope is quota. A call outside it is refused with the grant's own name in the message, so the author reads which scope to widen rather than that something went wrong.



## Related

- [Every capability](/nomercy-plugins/capabilities)
- [Runtime and isolation](/nomercy-plugins/handbook/runtime-and-isolation)
