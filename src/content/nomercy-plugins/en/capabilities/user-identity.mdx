---
title: user.identity
description: the caller's id, display name, role and access answer
tags: [capability, user-identity]
---

{/* Generated from tools/plugin-contract/contract/capabilities.json. Do not edit. */}

# `user.identity`

The caller's id, display name, role and access answer.

## What it covers

| | |
| --- | --- |
| Facade | `IPluginContext.User.Identity` |
| Scope | (always) |
| Trust | low |
| Revocable while the plugin runs | yes |

The owner grants this without being asked a second time.

Taking this grant away stops the next call. The plugin keeps running.

## Asking for it

Name it in the manifest:

```json
{
  "capabilities": [
    { "name": "user.identity", "scope": "*" }
  ]
}
```

The scope is (always). A call outside it is refused with the grant's own name in the message, so the author reads which scope to widen rather than that something went wrong.

## When it refuses

| Code | Severity | What happened |
| --- | --- | --- |
| `PLUGIN_USER_SCOPE_REQUIRED` | blocked | The call needs a user scope and ran without one. |

Every refusal carries what the plugin did, why the server said no, and how to fix it. Read all three before changing code.


## What the analyzer catches first

| Rule | What it says |
| --- | --- |
| `NMP0012` | User data is written outside the per-user scope. Use context.Storage.ForUser. |

These fire at build time, so the author sees them before the server ever refuses.


## Related

- [Every capability](/nomercy-plugins/capabilities)
- [Runtime and isolation](/nomercy-plugins/handbook/runtime-and-isolation)
