---
title: Music Player
description: nomercy-music-player v2 — Music Player
category: Music Player
order: 1
---

# nomercy-music-player — Documentation Home

Headless reference music player built on `nomercy-player-core`. Ships a dual-buffer WebAudio crossfade engine, a pluggable adapter system, and a typed plugin framework.

---

## What this package provides

- `NMMusicPlayer<T>` — the core class. Generic over your track type.
- `nmMPlayer(id)` — factory that creates or retrieves a named player instance.
- Six music-specific adapter ports (audio backend, playlist generator, scrobbler, lyric source, similarity engine, now-playing art).
- Eighteen built-in plugins (crossfade, auto-advance, group listening, EQ, spectrum, lyrics, MediaSession, and more).
- Full TypeScript types for all public methods, events, and configuration.

---

## Quick navigation

| Topic | Page |
|-------|------|
| Install + first track | [Quick Start](/player/music/quickstart) |
| All config options | [Configuration](/player/music/configuration) |
| All public methods | [API Methods](/player/music/api-methods) |
| All events + payload shapes | [Events](/player/music/events) |
| Vue integration | [Framework — Vue](/player/music/framework-vue) |
| React integration | [Framework — React](/player/music/framework-react) |
| Crossfade | [Crossfade](/player/music/crossfade) |
| Equalizer | [Equalizer](/player/music/equalizer) |
| Lyrics | [Lyrics](/player/music/lyrics) |
| Migrating from v1 | [Migration Guide](/player/music/migration-v1-v2) |
| Kit adapter catalog | [Kit — Adapters](/player/kit/adapters) |

### Plugins

These are built-in plugins shipped with `nomercy-music-player`. Add them with `player.addPlugin(PluginClass)`.

| Plugin | Purpose |
|--------|---------|
| `AutoAdvancePlugin` | Advance queue automatically when a track ends |
| `MediaSessionPlugin` | Wire lock screen and notification media controls |
| `LyricsPlugin` | Sync LRC/VTT lyrics to playback time |
| `KeyHandlerPlugin` | Keyboard shortcut handling |
| `CastSenderPlugin` | Chromecast handoff |
| `EmbedPlugin` | postMessage bridge for iframe embeds |
| `TabLeaderPlugin` | Single-tab leader election for multi-tab sync |
| `AudioGraphPlugin` | Web Audio routing graph (required for EQ/Spectrum) |
| `EqualizerPlugin` | 10-band parametric EQ (requires `AudioGraphPlugin`) |
| `SpectrumPlugin` | Frequency spectrum analyzer |
| `VisualizationPlugin` | Audio visualization canvas |
| `DrmPlugin` | DRM/EME support (planned) |
| `LiveTranscodingPlugin` | Live transcode from NoMercy server (planned) |
| `GroupListeningPlugin` | Synchronized group listening session (planned) |

Also see [Writing your own plugins](/player/music/plugin-development).

---

## Package coordinates

```
npm install @nomercy-entertainment/nomercy-music-player
```

npm name: `@nomercy-entertainment/nomercy-music-player`
GitHub: [NoMercy-Entertainment/nomercy-music-player](https://github.com/NoMercy-Entertainment/nomercy-music-player)

---

## Architecture in one diagram

```
Your track type (T extends MusicPlaylistItem)
       |
NMMusicPlayer<T>        ← per-library layer (music concerns only)
       |
nomercy-player-core     ← kit (generic: queue, auth, plugins, i18n, storage)
       |
IAudioBackend           ← backend layer (WebAudioBackend / AudioElementBackend)
       |
Web Audio API / <audio>
```

Everything below `NMMusicPlayer<T>` is generic — zero knowledge of your server API, your track shape beyond the typed fields, or any NoMercy infrastructure. App-specific glue lives in layer-5 consumer plugins.
