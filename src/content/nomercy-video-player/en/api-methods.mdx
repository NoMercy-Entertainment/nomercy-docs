---
title: "API Methods"
description: "All public methods on NMVideoPlayer. Signatures, overloaded getter/setter pattern, and examples."
category: "Getting Started"
order: 4
---

# API Methods Reference

All public methods on `NMVideoPlayer<T>`. Methods follow the overloaded getter/setter pattern: `method()` reads, `method(value)` writes.

---

## Factory

### `nmplayer(id?)`

```ts
nmplayer(id?: string | number): NMVideoPlayer<VideoPlaylistItem>
nmplayer<T extends VideoPlaylistItem>(id?: string | number): NMVideoPlayer<T>
```

Creates or retrieves a named player instance.

- `id` is optional. When omitted, the kit generates a unique id.
- Two calls with the same `id` return the **same** instance — no re-initialization.
- The factory is the only entry point. Do not call `new NMVideoPlayer()` directly.
- Instances persist until `dispose()` is called. After dispose, the same `id` creates a fresh instance.
- When `setup({ expose: true })` is called, `window.nmplayer` is set to the factory for console access.

```ts
const player = nmplayer('main');   // creates or retrieves 'main'
const same   = nmplayer('main');   // same instance
player === same; // true
```

---

## Setup

### `setup(opts)`

```ts
setup(opts: VideoPlayerConfig<T>): NMVideoPlayer<T>
```

Initialize the player. Returns the player for chaining. See [Configuration](/player/video/configuration) for all options.

### `ready()`

```ts
ready(): Promise<void>
```

Resolves when setup is complete and all plugins' `use()` promises have resolved.

### `dispose()`

```ts
dispose(): void
```

Tears down the player: stops playback, disposes all plugins, removes event listeners. The instance is removed from the internal registry.

---

## Transport

### `play(opts?)`, `pause(opts?)`, `stop(opts?)`, `restart(opts?)`

```ts
play(opts?: ActionOptions): Promise<void>
pause(opts?: ActionOptions): Promise<void>
stop(opts?: ActionOptions): Promise<void>
restart(opts?: ActionOptions): Promise<void>
togglePlayback(opts?: ActionOptions): Promise<void>
```

`stop()` resets position to 0. `restart()` seeks to 0 and plays. `togglePlayback()` switches between play and pause.

`ActionOptions` carries `source: 'user' | 'auto' | 'remote'` — pass `{ source: 'user' }` when triggered by user input.

---

## Queue navigation

### `next(opts?)`, `previous(opts?)`

```ts
next(opts?: ActionOptions): Promise<void>
previous(opts?: ActionOptions): Promise<void>
```

### `seekToIndex(index, opts?)`

```ts
seekToIndex(index: number, opts?: ActionOptions): void
```

Jump to a queue position by index.

### `peekNext()`, `peekPrevious()`

```ts
peekNext(): T | undefined
peekPrevious(): T | undefined
```

---

## Current item

### `current()`

```ts
current(): T | undefined
```

Return the currently loaded item.

### `current(item, opts?)`

```ts
current(item: T, opts?: ActionOptions): void
```

Set the current item. The item must be in the queue.

### `currentIndex()`

```ts
currentIndex(): number
```

Index of the current item. Returns `-1` if nothing is loaded.

### `load(item, opts?)`

```ts
load(item: T, opts?: LoadOptions): Promise<void>
```

Load a single item without requiring it to be in the queue.

---

## Time

### `currentTime()` / `currentTime(t, opts?)`

```ts
currentTime(): number
currentTime(t: number, opts?: ActionOptions): void
```

Get or set the playback position in seconds.

### `duration()`

```ts
duration(): number
```

### `buffered()`, `bufferedRanges()`

```ts
buffered(): number
bufferedRanges(): TimeRanges
```

### `seekable()`

```ts
seekable(): TimeRanges
```

### `forward(s?, opts?)`, `rewind(s?, opts?)`

```ts
forward(s?: number, opts?: ActionOptions): Promise<void>
rewind(s?: number, opts?: ActionOptions): Promise<void>
```

Skip forward/backward by `s` seconds. Default: 10.

### `seekByPercentage(pct, opts?)`

```ts
seekByPercentage(pct: number, opts?: ActionOptions): void
```

Seek to a percentage (0–100) of total duration.

### `timeData()`

```ts
timeData(): { time: number; duration: number; buffered: number; seekable: TimeRanges; playbackRate: number }
```

---

## Volume

### `volume()` / `volume(v)`

```ts
volume(): number
volume(v: number): void
```

Get or set volume. **Scale: 0–100.**

Returns 0 when muted (regardless of stored pre-mute level). `volume(v)` clamps to [0, 100] and emits `volume`. No-op if cancelled by `beforeMutation`.

```ts
player.volume(80);         // 80%
const level = player.volume(); // 80
```

### `mute()`, `unmute()`, `toggleMute()`

```ts
mute(): void
unmute(): void
toggleMute(): void
volumeUp(step?: number): void    // step default: 5 percentage points
volumeDown(step?: number): void  // step default: 5 percentage points
```

`mute()` saves the current level so `unmute()` can restore it exactly. Both are no-ops when already in the target state.

### `volumeState()`

```ts
volumeState(): VolumeState  // 'muted' | 'unmuted'
```

---

## Playback rate

### `playbackRate()` / `playbackRate(r)`

```ts
playbackRate(): number
playbackRate(r: number): void
playbackRates(): number[]
```

---

## Quality levels

### `qualityLevels()`

```ts
qualityLevels(): QualityLevel[]
```

All available quality levels (from HLS.js after manifest parse).

### `currentQuality()` / `currentQuality(idx)`

```ts
currentQuality(): number     // -1 = auto
currentQuality(idx: number): void  // -1 to re-enable auto
```

### `qualityState()`

```ts
qualityState(): QualityState
```

---

## Audio tracks

### `audioTracks()`

```ts
audioTracks(): AudioTrack[]
```

Available audio tracks exposed by the HLS manifest.

### `currentAudioTrack()` / `currentAudioTrack(index)`

```ts
currentAudioTrack(): number | null
currentAudioTrack(index: number): void
```

Get the active audio track index, or set it by index.

### `cycleAudioTracks()`

```ts
cycleAudioTracks(): void
```

Cycle to the next available audio track. Wraps around.

---

## Subtitles

### `subtitles()`

```ts
subtitles(): SubtitleTrack[]
```

All subtitle tracks for the current item.

### `currentSubtitle()` / `currentSubtitle(index)`

```ts
currentSubtitle(): number | null
currentSubtitle(index: number | null): void
```

Get the active subtitle track index (`null` = off), or set by index. Pass `null` to disable subtitles.

### `subtitleState()`

```ts
subtitleState(): SubtitleState  // 'on' | 'off'
```

Whether any subtitle track is currently active. Checks both the kit selection and `videoElement.textTracks` for backends that toggle native tracks directly.

### `subtitleStyle()` / `subtitleStyle(patch)`

```ts
subtitleStyle(): SubtitleStyle
subtitleStyle(patch: Partial<SubtitleStyle>): void
```

Read or write subtitle style preferences. Write merges the patch onto the current style and emits `subtitleStyle`. Persistence is the responsibility of preference plugins — the player only keeps in-memory state.

### `cycleSubtitles()`

```ts
cycleSubtitles(): void
```

Walk: off → track 0 → track 1 → ... → last track → off.

---

## Chapters

### `chapters()`

```ts
chapters(): Chapter[]
```

All chapters for the current item. Returns an empty array when no chapters are loaded.

### `currentChapter()` / `currentChapter(idx)`

```ts
currentChapter(): Chapter | null
currentChapter(idx: number): void
```

Get the active chapter at `currentTime()` (returns `null` when not in any chapter range), or seek to a chapter by index.

### `seekToChapter(idx, opts?)`

```ts
seekToChapter(idx: number, opts?: ActionOptions): void
```

Seek to the start of a chapter by index. Equivalent to `currentTime(chapters()[idx].startTime)`.

### `nextChapter(opts?)`

```ts
nextChapter(opts?: ActionOptions): void
```

Advance to the start of the next chapter.

### `previousChapter(opts?)`

```ts
previousChapter(opts?: ActionOptions): void
```

Return to the start of the previous chapter (or the start of the current chapter if already past its midpoint).

---

## Fullscreen / PiP / Theater

### `fullscreenState()` / `fullscreenState(state)`

```ts
fullscreenState(): FullscreenState
fullscreenState(state: FullscreenState | boolean): void
```

`FullscreenState` is `'on' | 'off'`. Pass `true`/`false` or the enum value. Requires a `fullscreen` controller in the `platform` option (wired automatically by `browserPlatform`). Throws `BrowserPolicyError` (`core:policy/fullscreenUnsupported`) if no controller is configured.

```ts
player.fullscreenState(true);   // enter
player.fullscreenState(false);  // exit
player.fullscreenState();       // FullscreenState.ON | FullscreenState.OFF
```

### `toggleFullscreen()`

```ts
toggleFullscreen(): void
```

Toggle fullscreen on/off. Shorthand for `fullscreenState(!isActive)`.

### `pipState()` / `pipState(state)`

```ts
pipState(): PipState
pipState(state: PipState | boolean): void
```

`PipState` is `'on' | 'off'`. Requires a `pip` controller. Throws `BrowserPolicyError` (`core:policy/pipUnsupported`) if unavailable.

### `togglePip()`

```ts
togglePip(): void
```

### `theaterState()` / `theaterState(state)`

```ts
theaterState(): TheaterState
theaterState(state: TheaterState | boolean): void
```

`TheaterState` is `'on' | 'off'`. Pure player-level state — the UI plugin reacts via the `theater` event.

### `toggleTheater()`

```ts
toggleTheater(): void
```

---

## Aspect ratio and stretching

### `aspectRatio()` / `aspectRatio(value)`

```ts
aspectRatio(): 'uniform' | 'fill' | 'exactfit' | 'none'
aspectRatio(value: 'uniform' | 'fill' | 'exactfit' | 'none'): void
```

| Value | CSS `object-fit` | Behavior |
|-------|-----------------|----------|
| `'uniform'` | `contain` | Letterbox/pillarbox — preserve aspect ratio |
| `'fill'` | `fill` | Stretch to fill, ignore aspect ratio |
| `'exactfit'` | `cover` | Cover without bars, may crop |
| `'none'` | `none` | No scaling |

### `cycleAspectRatio()`

```ts
cycleAspectRatio(): void
```

Cycle through `'uniform' → 'fill' → 'exactfit' → 'none' → 'uniform'`. Emits `aspectRatio`.

---

## Shuffle and repeat

### `shuffleState()` / `shuffleState(state)`

```ts
shuffleState(): ShuffleState
shuffleState(state: ShuffleState): void
```

### `repeatState()` / `repeatState(state)`

```ts
repeatState(): RepeatState
repeatState(state: RepeatState): void
```

---

## Queue management

All queue methods mirror the music player. See [Music — API Methods](/player/music/api-methods) for the full queue method signatures — `queue`, `queueAppend`, `queuePrepend`, `queueInsert`, `queueRemove`, `queueRemoveAt`, `queueMove`, `queueClear`, `queueShuffle`, `queueSort`, `queueIndexOf`, `queueLength`.

### `loadQueue(url, parser?)`

```ts
loadQueue(url: string, parser?: (response: Response) => Promise<T[]>): Promise<void>
```

Fetch a URL and replace the queue. Uses the player's auth pipeline. Optionally supply a custom parser if the response is not a raw JSON array.

---

## Plugins

```ts
addPlugin<P>(Cls: PluginClass<P>, opts?: PluginOptions<P>): NMVideoPlayer<T>
getPlugin<P>(Cls: PluginClass<P>): P | undefined
getPluginById(id: string): Plugin | undefined
removePlugin<P>(Cls: PluginClass<P>, opts?: { cascade?: boolean }): void
plugins(): Plugin[]
enabledPlugins(): Plugin[]
```

---

## Phase and diagnostics

### `phase()`

```ts
phase(): PlayerPhase
```

Current phase: `'idle' | 'setup' | 'ready' | 'loading' | 'starting' | 'playing' | 'paused' | 'buffering' | 'seeking' | 'ended' | 'stopped' | 'disposing' | 'disposed'`.

### `playState()`

```ts
playState(): PlayState  // 'idle' | 'loading' | 'playing' | 'paused' | 'stopped' | 'error'
```

### `metrics()`

```ts
metrics(): PlaybackMetrics
```

Snapshot of playback metrics. `ttfb`, `avgBitrate`, and `decoderStalls` are `null` on the html5 backend — they are not currently wired. `droppedFrames` is `number` (from `getVideoPlaybackQuality()`). Guard nullable fields before use.

### `bandwidth()`

```ts
bandwidth(): number
```

Current estimated bandwidth in bits/sec (from hls.js).

### `now()`

```ts
now(): number
```

Monotonic clock. Uses `options.clockSource()` if configured, else `Date.now()`.

---

## Backend

### `backend()`

```ts
backend(): IVideoBackend
```

Returns the active video backend. Instantiates `Html5VideoBackend` (or the `backendFactory` result) on first call. This is the lowest-level surface — consumers rarely need it directly.

---

## Stream registration

### `registerStream(factory, prepend?)`

```ts
registerStream(factory: StreamFactory, prepend?: boolean): this
```

Register a custom stream factory (e.g. DASH, SRT). Pass `true` to prepend at higher priority than HLS. Returns the player for chaining.

### `unregisterStream(id)`

```ts
unregisterStream(id: string): this
```

Remove a registered stream factory by id.

### `streams()`

```ts
streams(): ReadonlyArray<string>
```

Names of all registered stream factories in priority order.

### `getStreamFactory(id)`

```ts
getStreamFactory(id: string): StreamFactory | undefined
```

---

## Cue parsers

### `registerCueParser(parser, prepend?)`

```ts
registerCueParser(parser: CueParser, prepend?: boolean): void
```

Register a custom subtitle/chapter cue parser. Parsers are tried in registration order; the first one that returns a non-null result wins.

### `unregisterCueParser(id)`

```ts
unregisterCueParser(id: string): void
```

### `resolveCueParser(url)`

```ts
resolveCueParser(url: string): CueParser | undefined
```

---

## Device capabilities

### `isTv()`, `isMobile()`, `isDesktop()`

```ts
isTv(): boolean
isMobile(): boolean
isDesktop(): boolean
```

Device type detection. Used internally for buffer cap tiers and ABR constraints.

### `device()`

```ts
device(): DeviceCapabilities
```

Full device capability snapshot: `{ isTv, isMobile, isDesktop, hasPointer, hasTouch, ... }`.

### `canPlay(profile)`

```ts
canPlay(profile: {
  contentType: string;
  width?: number;
  height?: number;
  bitrate?: number;
  framerate?: number;
}): Promise<CanPlayResult>
```

Query `MediaCapabilities.decodingInfo()` for a given format profile. Used internally for HDR-aware ABR. `CanPlayResult` extends `MediaCapabilitiesDecodingInfo`.

---

## Bandwidth estimation

### `bandwidth()`

```ts
bandwidth(): number
```

Current estimated bandwidth in bits/sec (from hls.js). Returns `0` before any segment has loaded.

### `bandwidthEstimator()` / `bandwidthEstimator(fn)`

```ts
bandwidthEstimator(): (() => number) | undefined
bandwidthEstimator(fn: () => number): void
```

Get or replace the bandwidth estimator function. The estimator is polled by the ABR layer. Supply a custom function to bias quality selection based on your own network signal.

---

## Audio output devices

### `audioOutputs()`

```ts
audioOutputs(): Promise<MediaDeviceInfo[]>
```

Enumerate available audio output devices. Requires the user to have granted device-access permission.

### `selectAudioOutput()`

```ts
selectAudioOutput(): Promise<MediaDeviceInfo | null>
```

Show the browser's audio output device picker. Returns the selected device, or `null` if the user cancelled.

### `currentAudioOutput()` / `currentAudioOutput(deviceId)`

```ts
currentAudioOutput(): Promise<string | null>
currentAudioOutput(deviceId: string): Promise<void>
```

Get or set the active audio output device id.

---

## Auth

### `auth()` / `auth(config)`

```ts
auth(): Readonly<AuthConfig> | undefined
auth(config: AuthConfig): void
auth(partial: Partial<AuthConfig>): void
```

Read or merge auth config at runtime. Pass a full `AuthConfig` to replace, or a `Partial<AuthConfig>` to merge only specified fields (e.g. to update a refreshed token).

### `refreshAuth()`

```ts
refreshAuth(): Promise<void>
```

Trigger a manual auth refresh. Calls `auth.refreshOnUnauthenticated()` and updates the stored token.

---

## URL

### `baseUrl()` / `baseUrl(url)`

```ts
baseUrl(): string | undefined
baseUrl(url: string): void
```

Get or set the base URL prepended to relative media source URLs. Separate from `imageBasePath` (which applies to artwork).

### `resolveUrl(url, category?)`

```ts
resolveUrl(url: string, category?: UrlCategory): Promise<ResolvedUrl>
```

Resolve a URL through the configured `urlResolver` (or the built-in `auth.transformUrl` + parse default). Use for URLs that cannot carry `Authorization` headers (Cast, Worker, CSS) — auth travels via signed URL or query string through the resolver.

`UrlCategory` values: `'media' | 'subtitle' | 'poster' | 'thumbnail' | 'chapter' | 'license' | 'cast'`.

### `urlResolver()` / `urlResolver(fn)`

```ts
urlResolver(): UrlResolver | undefined
urlResolver(resolver: UrlResolver | undefined): void
```

---

## i18n

### `t(key, vars?)`

```ts
t(key: string, vars?: Record<string, string>): string
```

Translate a key using the configured translator. Missing keys fall back to the key itself (or the `onMissingTranslation` handler).

### `language()` / `language(lang)`

```ts
language(): string
language(lang: string): Promise<void>
```

Get or set the active language (BCP-47). Setting a new language triggers `loadTranslations` in all plugins.

### `addTranslations(bundle)`

```ts
addTranslations(bundle: Translations): void
```

Merge a translation bundle (`{ en: { key: 'value' }, nl: { ... } }`) into the player's table at runtime.

### `removeTranslations(prefix, lang?)`

```ts
removeTranslations(prefix: string, lang?: string): void
```

Remove all keys with the given prefix from the translation table. Optionally scoped to one language.

### `translation(lang, key)` / `translation(lang, key, value)`

```ts
translation(lang: string, key: string): string | undefined
translation(lang: string, key: string, value: string): void
```

Low-level per-key read or write. Plugins use `t()` instead.

---

## Cast

### `castState()`

```ts
castState(): CastState
```

### `transferTo(target)`

```ts
transferTo(target: 'cast' | 'airplay' | 'remote-playback'): Promise<void>
```

Hand off playback to a cast target. The `CastSenderPlugin` must be registered for `'cast'`.

---

## Preload and transition strategies

### `setPreloadStrategy(strategy)`

```ts
setPreloadStrategy(strategy: PreloadStrategy): void
```

Replace the active preload strategy. Default: `VideoPreloadStrategy` (preloads 10 s before end).

### `preloadStrategy()`

```ts
preloadStrategy(): PreloadStrategy
```

### `setTransitionStrategy(strategy)`

```ts
setTransitionStrategy(strategy: TransitionStrategy): void
```

Replace the transition strategy. Default: `GaplessTransitionStrategy` (hard cut, no crossfade).

### `transitionStrategy()`

```ts
transitionStrategy(): TransitionStrategy
```

---

## Phase and diagnostics

### `phase()`

```ts
phase(): PlayerPhase
```

Current phase: `'idle' | 'setup' | 'ready' | 'loading' | 'starting' | 'playing' | 'paused' | 'buffering' | 'seeking' | 'ended' | 'stopped' | 'disposing' | 'disposed'`.

### `setupState()`

```ts
setupState(): SetupState
```

Snapshot of the setup pipeline state. Useful for debugging plugin init ordering.

### `dispatching()`

```ts
dispatching(): ReadonlyArray<string>
```

Names of all active `before*` events currently in flight. Used by plugins that want to block re-entrant mutations.

### `platform()`

```ts
platform(): IPlatform
```

The configured platform bundle (`fullscreen`, `pip`, `wakeLock`, `network`, `visibility` controllers).

### `playState()`

```ts
playState(): PlayState  // 'idle' | 'loading' | 'playing' | 'paused' | 'stopped' | 'error'
```

### `bufferState()`

```ts
bufferState(): BufferState
```

### `networkState()`

```ts
networkState(): NetworkState
```

### `visibilityState()`

```ts
visibilityState(): VisibilityState  // 'visible' | 'hidden'
```

### `streamState()`

```ts
streamState(): string
```

Active stream factory id (e.g. `'hls'`, `'native'`).

### `metrics()`

```ts
metrics(): PlaybackMetrics
```

Playback metrics snapshot. `droppedFrames` comes from `getVideoPlaybackQuality()`. `ttfb`, `avgBitrate`, `decoderStalls` are `null` on the Html5 backend — they are not wired. Guard nullable fields before use.

### `recordMetric(name, value)`

```ts
recordMetric(name: string, value: number): void
```

Record a custom counter. Merged into the `metrics()` snapshot.

### `bandwidth()`

```ts
bandwidth(): number
```

Current estimated bandwidth in bits/sec.

### `now()`

```ts
now(): number
```

Monotonic clock. Uses `options.clockSource()` if configured, else `Date.now()`. Use for distributed time sync.

---

## Auth

### `auth()` / `auth(config)`

See the full auth section above.

---

## Accessibility

### `announce(text, level?)`

```ts
announce(text: string, level?: 'polite' | 'assertive'): void
```

Post a message to the ARIA live region so screen readers announce it. Default level: `'polite'`.

---

## DOM helpers (for plugin authors)

```ts
createElement(tag: string, attrs?: Record<string, string>): HTMLElement
createButton(label: string, onClick: () => void): HTMLButtonElement
createSVG(path: string): SVGElement
addClasses(el: HTMLElement, ...classes: string[]): void
removeClasses(el: HTMLElement, ...classes: string[]): void
```

Low-level DOM construction helpers exposed to plugins. Not typically used in application code.

---

## Back listener

The `back` event is how the `DesktopUiPlugin` decides whether to show the back button. Register a listener for it to signal that a back action is possible:

```ts
player.on('back', () => {
  router.back();
});
```

`DesktopUiPlugin` calls `player.hasListeners('back')` during `ready()`. If at least one listener is registered, the back button appears in the top-left corner. If no listener is registered, the button is not shown.

There is no `setBackListener()` method — use `player.on('back', fn)` and `player.off('back', fn)` directly.

---

## Event API

```ts
on<K extends keyof VideoEventMap>(event: K, handler: (payload: VideoEventMap[K]) => void): void
off<K extends keyof VideoEventMap>(event: K, handler: (payload: VideoEventMap[K]) => void): void
once<K extends keyof VideoEventMap>(event: K, handler: (payload: VideoEventMap[K]) => void): void
on<P extends Plugin, K extends keyof P['__events__']>(Cls: PluginClass<P>, event: K, handler: ...): void
```
