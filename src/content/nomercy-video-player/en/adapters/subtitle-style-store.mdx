---
title: 'Subtitle Style Store'
description: 'The ISubtitleStyleStore adapter for persisting subtitle preferences.'
draft: false
---

{/* Source: packages/nomercy-video-player-v2/src/adapters/subtitle-style-store/ISubtitleStyleStore.ts */}

## What is a subtitle style store?

`ISubtitleStyleStore` is the adapter the player uses to persist and restore the user's subtitle style preferences across page reloads. The default implementation (`StorageBackedSubtitleStyleStore`) uses the kit's `IStorage` adapter so the storage backend is swappable.

## ISubtitleStyleStore interface

```ts
import type { ISubtitleStyleStore } from '@nomercy-entertainment/nomercy-video-player';
```

| Method        | Signature                                       | Description                                                                          |
| ------------- | ----------------------------------------------- | ------------------------------------------------------------------------------------ |
| `load()`      | `() => Promise<Partial<SubtitleStyle> \| null>` | Load the persisted style. Returns `null` when no preference has been saved           |
| `save(style)` | `(style: SubtitleStyle) => Promise<void>`       | Persist the full merged style. Called after every `player.subtitleStyle(patch)` call |
| `clear()`     | `() => Promise<void>`                           | Clear persisted preferences (e.g. "reset to defaults")                               |

## When is this called?

- `load()` — once during `setup()`. The player applies the returned style as the initial `SubtitleStyle` state before any subtitle tracks are activated.
- `save(style)` — after every `player.subtitleStyle(patch)` call, with the full merged style.
- `clear()` — consumer-triggered, e.g. a "Reset subtitle preferences" button in a settings screen.

## SubtitleStyle

`SubtitleStyle` is defined in `@nomercy-entertainment/nomercy-player-core` and covers all user-facing subtitle appearance options:

| Field               | Type     | Description                                                                                         |
| ------------------- | -------- | --------------------------------------------------------------------------------------------------- |
| `fontFamily`        | `string` | CSS font-family string                                                                              |
| `fontSize`          | `number` | Scale percentage (100 = default size)                                                               |
| `textColor`         | `string` | CSS color string for subtitle text                                                                  |
| `textOpacity`       | `number` | 0–100 opacity for text                                                                              |
| `backgroundColor`   | `string` | CSS color string for text background                                                                |
| `backgroundOpacity` | `number` | 0–100 opacity for text background                                                                   |
| `edgeStyle`         | `string` | Text shadow style: `'none'`, `'dropShadow'`, `'raised'`, `'depressed'`, `'uniform'`, `'textShadow'` |
| `areaColor`         | `string` | CSS color string for the subtitle area background                                                   |
| `windowOpacity`     | `number` | 0–100 opacity for the area background                                                               |

## Reading and writing style

```ts
// Read current style
const style = player.subtitleStyle();

// Merge a partial update — triggers save() on the adapter
player.subtitleStyle({
  fontSize: 120,
  textColor: 'yellow',
});
```

The `'subtitleStyle'` event fires after each update, carrying the new full style. The SubtitleOverlayPlugin subscribes to this event to repaint active cue areas immediately.

## See also

- [SubtitleOverlay plugin](/nomercy-video-player/plugins/subtitle-overlay)
- [Player Kit — adapters](/nomercy-player-kit/adapters)
- [Types Reference](/nomercy-video-player/types)
