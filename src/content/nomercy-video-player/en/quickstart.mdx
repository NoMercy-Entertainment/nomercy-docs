---
title: 'Quick Start'
description: 'Install nomercy-video-player and play a real HLS video with the baseUrl pattern. One copy-paste example that works first try.'
---

# Video Player: Quick Start

This gets a real video loaded and ready in the preview below: Sintel, streamed over HLS, using the baseUrl pattern every NoMercy player and app is built around.

## Install

```bash
npm install @nomercy-entertainment/nomercy-video-player
```

That's the only package you need. `hls.js` is a regular dependency pulled in through `nomercy-player-core`, so npm installs it for you; the player dynamically imports it for every browser except Safari, which plays HLS natively. There is no peer dependency to add by hand.

## Play your first video

Here it is loaded for real: Sintel, streamed over HLS, ready on the first frame. Press play in the preview to start it. Below the player is the exact code that produced it, ready to copy:

:::snippet{file="quickstart" runnable preview="first"}
:::

Add a `<div id="player"></div>` to your page for that code to mount into.

The player loads the first item as it mounts, so the preview above sits ready on Sintel's first frame with nothing set to autoplay. The `controls` flag gives it the native play button you press to start.

`autoPlay` only decides whether that already-loaded item also begins playing. `muted` pairs with it, because browsers block autoplay with sound.

Leave both off, as this example does, and drive playback from your own UI with `player.play()` on a user gesture.

`nmplayer('player')` mounts into `document.getElementById('player')`, or returns the instance already registered under that id. Calling it twice is always safe.

## The baseUrl pattern

`baseUrl` is the one config field every playlist should set. Item paths (`url` above) stay relative, exactly the shape a real media server sends, and the player prepends `baseUrl` as a plain string prefix, keeping the whole base path:

- The server sends the base with no trailing slash (`.../master/Films`) and each item path with a leading slash (`/Sintel.(2010)/...`).
- Move to a different server, a different environment, or a signed-URL CDN, and you change one config value; nothing in your catalogue is hardcoded to today's origin.
- Never pre-join an absolute URL into an item yourself. That is the player's job, and doing it twice breaks the moment the base changes.

## Next steps

- [Guided Tour: Transport](/nomercy-video-player/tour/transport): play(), pause(), and seeking, the first stop on the full method surface.
- [Build a Player](/nomercy-video-player/build/shell): put a real overlay UI on top of the player, one control at a time.
