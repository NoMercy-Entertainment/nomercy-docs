---
title: 'Autostart'
description: 'Control whether the server starts with the machine.'
draft: false
---

# Autostart

A media server is expected to be there. Autostart makes it come back on its own after a reboot or a power cut, without someone signing in to start it.

## Check

```bash
nomercy autostart status
```

## Turn it on

```bash
nomercy autostart enable
```

## Turn it off

```bash
nomercy autostart disable
```

## What it does per platform

On Windows the server is registered to start with the machine. On macOS it runs as a launch agent. On Linux it runs as a systemd service.

A server installed from a Linux package or run in Docker already starts on boot, so there is nothing to enable there. Docker handles it through the `restart: unless-stopped` policy in the compose file.

## Where to go next

- [Starting and stopping](/nomercy-media-server/cli/start-stop)
