---
title: "Skeleton"
description: "Three variants: text, circle, and rectangle for different content types"
---

`NMSkeleton` is the component your server names when it wants a skeleton.
Like every NM component it accepts children and takes its box from the payload, so you can place
one inside a dashboard without it needing to know where it was placed.

## Sending one

A component arrives inside the standard envelope. The `component` field names it and `props` is
everything below.

```json
{
  "id": "skeleton-1",
  "component": "NMSkeleton",
  "props": {
    "id": "skeleton-1",
    "variant": "rectangle",
    "box": { "margin": { "y": "4" }, "padding": { "all": "3" } }
  }
}
```

## What it does

These are the behaviours the design system specifies for it, so you can rely on all of them
being present on web, on mobile and on television.

- Three variants: text, circle, and rectangle for different content types
- Pulse animation indicates loading state
- Customizable dimensions via inline width and height styles
- role="status" for screen reader announcements
- aria-busy="true" indicates content is loading
- aria-label="Loading content" provides accessible context
- .sr-only loading text for additional screen reader support
- Non-interactive placeholder , no focus or keyboard handling

## Options

Every option is optional. Leave one out and the component uses the default in the last column,
which is the same default on every platform.

| Option | Accepts | Default | What it does |
| --- | --- | --- | --- |
| `variant` | `text` , `circle` , `rectangle` | `text` | Skeleton shape |

## Children

Anything you nest under `items` renders inside it.

## Accessibility

You do not have to do anything to get this. It is built in, and the list is here so you know what
you can count on and what you would be breaking if you overrode it.

| Attribute | Why |
| --- | --- |
| `role="status"` | Identifies the element as a live region that announces loading state to assistive technologies |
| `aria-busy="true"` | Indicates content is currently loading |
| `aria-label="Loading content"` | Provides an accessible name describing the loading context |
| `.sr-only` | Visually hidden "Loading..." text for additional screen reader support |
| `Non-interactive` | Skeleton is a decorative placeholder; no focus or keyboard interaction |

### Keyboard and remote

Each of these works with a keyboard on web and with a D-pad on television.

| Key | What happens |
| --- | --- |
| `Tab` | Focus skips over skeleton placeholders; they are not focusable |
| `Shift + Tab` | Focus moves to previous element; skeletons are skipped |
| `No interaction` | Skeleton is non-interactive; no Enter, Space, or other keys apply |

## Placing it

Padding, margin, width, alignment and order all come from `box`, which every component accepts.
A parent decides where this component sits and how much room it takes; the component decides how
its own children are arranged. See [the box](/nm-components/box) for the full vocabulary.
