---
title: "Breadcrumb"
description: "Breadcrumb is part of the NM component set."
---

`NMBreadcrumb` is the component your server names when it wants a breadcrumb.
Like every NM component it accepts children and takes its box from the payload, so you can place
one inside a dashboard without it needing to know where it was placed.

## Sending one

A component arrives inside the standard envelope. The `component` field names it and `props` is
everything below.

```json
{
  "id": "breadcrumb-1",
  "component": "NMBreadcrumb",
  "props": {
    "id": "breadcrumb-1",
    "separator": "dot",
    "box": { "margin": { "y": "4" }, "padding": { "all": "3" } }
  }
}
```

The same thing from a server, or from a plugin, using the generated builder:

```csharp
NMBreadcrumbProps props = new()
{
    Id = "breadcrumb-1",
    Separator = "dot",
    Box = new() { Margin = new() { Y = NmSpace.X4 }, Padding = new() { All = NmSpace.X3 } },
};

// In a plugin, the record becomes a node the client draws:
PluginComponent node = PluginDesign.Node("breadcrumb-1", props);
```

## Options

Every option is optional. Leave one out and the component uses the default in the last column,
which is the same default on every platform.

| Option | Accepts | Default | What it does |
| --- | --- | --- | --- |
| `labels` | text | `['Home', 'Products', 'Current']` | Array of breadcrumb labels |
| `separator` | `slash` , `chevron` , `dot` | `chevron` | Separator style |

## Children

Anything you nest under `items` renders inside it.

## Accessibility

You do not have to do anything to get this. It is built in, and the list is here so you know what
you can count on and what you would be breaking if you overrode it.

| Attribute | Why |
| --- | --- |
| `aria-label="Breadcrumb"` | `Applied to <nav> to identify the landmark as a breadcrumb trail for screen readers` |
| `aria-current="page"` | On the last (current) item so assistive technologies announce it as the current page |
| `<nav>` | Semantic landmark for navigation; screen readers can list and jump to breadcrumb |
| `<ol> and <li>` | Ordered list conveys hierarchy and item count to assistive technologies |
| `href` | Required on ancestor links for valid navigation and keyboard activation |

### Keyboard and remote

Each of these works with a keyboard on web and with a D-pad on television.

| Key | What happens |
| --- | --- |
| `Tab` | Moves focus to the next link in the breadcrumb trail |
| `Shift + Tab` | Moves focus to the previous link |
| `Enter` | Activates the focused link and navigates |
| `Space` | Activates the focused link (when focus is on a link) |

## Placing it

Padding, margin, width, alignment and order all come from `box`, which every component accepts.
A parent decides where this component sits and how much room it takes; the component decides how
its own children are arranged. See [the box](/nm-components/box) for the full vocabulary.
