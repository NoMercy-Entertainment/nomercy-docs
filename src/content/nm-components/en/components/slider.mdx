---
title: "Sliders & Ranges"
description: "Sliders & Ranges is part of the NM component set."
---

`NMSlider` is the component your server names when it wants a sliders & ranges.
Like every NM component it accepts children and takes its box from the payload, so you can place
one inside a dashboard without it needing to know where it was placed.

## Sending one

A component arrives inside the standard envelope. The `component` field names it and `props` is
everything below.

```json
{
  "id": "slider-1",
  "component": "NMSlider",
  "props": {
    "id": "slider-1",
    "labelMode": "tooltip",
    "size": "lg",
    "box": { "margin": { "y": "4" }, "padding": { "all": "3" } }
  }
}
```

The same thing from a server, or from a plugin, using the generated builder:

```csharp
NMSliderProps props = new()
{
    Id = "slider-1",
    LabelMode = "tooltip",
    Size = "lg",
    Box = new() { Margin = new() { Y = NmSpace.X4 }, Padding = new() { All = NmSpace.X3 } },
};

// In a plugin, the record becomes a node the client draws:
PluginComponent node = PluginDesign.Node("slider-1", props);
```

## Options

Every option is optional. Leave one out and the component uses the default in the last column,
which is the same default on every platform.

| Option | Accepts | Default | What it does |
| --- | --- | --- | --- |
| `ariaLabel` | text | x | Accessible label |
| `disabled` | `true` , `false` | `false` | Disable slider interaction |
| `isBiValue` | `true` , `false` | `false` | Enable range mode with two thumbs |
| `labelMode` | `none` , `bottom` , `tooltip` | `none` | Label display mode |
| `size` | `sm` , `md` , `lg` | `md` | Track height |
| `value` | number | `0` | Current value (0-100) |
| `value2` | number | x | Second value for range mode |

## Children

Anything you nest under `items` renders inside it.

## What it reports

These are the moments the component tells the client about. In a payload you respond to them
with an `action`, which names an intent rather than carrying code.

| Event | Carries | When |
| --- | --- | --- |
| `change` | `{ value: number } \| { value: number, value2: number }` | Fired when value changes |

## Accessibility

You do not have to do anything to get this. It is built in, and the list is here so you know what
you can count on and what you would be breaking if you overrode it.

| Attribute | Why |
| --- | --- |
| `role="slider"` | Identifies the element as a slider to assistive technology |
| `aria-valuenow` | Current value (0–100). Required for sliders |
| `aria-valuemin` | Minimum value (typically 0) |
| `aria-valuemax` | Maximum value (typically 100) |
| `aria-label` | Provides an accessible name when context needs description (e.g., "Volume") |
| `aria-hidden="true"` | Use on visual label and tooltip spans so screen readers rely on aria-valuenow instead |
| `tabindex="0"` | Makes the thumb focusable for keyboard navigation |

### Keyboard and remote

Each of these works with a keyboard on web and with a D-pad on television.

| Key | What happens |
| --- | --- |
| `Arrow Left / Arrow Down` | Decrease the value |
| `Arrow Right / Arrow Up` | Increase the value |
| `Tab` | Moves focus to the next focusable element |
| `Shift + Tab` | Moves focus to the previous focusable element |

## Placing it

Padding, margin, width, alignment and order all come from `box`, which every component accepts.
A parent decides where this component sits and how much room it takes; the component decides how
its own children are arranged. See [the box](/nm-components/box) for the full vocabulary.
