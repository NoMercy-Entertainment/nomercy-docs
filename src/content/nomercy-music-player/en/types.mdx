---
title: 'Types Reference'
description: 'All exported types from @nomercy-entertainment/nomercy-music-player: MusicPlaylistItem, MusicPlayerConfig, IMusicPlayer, MusicEventMap, CrossfadeOptions, enums.'
draft: false
---

# Types Reference

Most types are exported from `@nomercy-entertainment/nomercy-music-player`. The audio-backend types (`AudioBackendFactory`, `BackendState`, `BackendLoaderState`, `BackendEvent`) import from the `@nomercy-entertainment/nomercy-music-player/adapters/audio-backend` subpath.

## Core types

### `MusicPlaylistItem`

```ts
interface MusicPlaylistItem extends BasePlaylistItem {
  id: string | number;
  name: string;
  url?: string;
  cover?: string;
  artist?: string;
  album?: string;
  lyricsUrl?: string;
  duration?: number;
}
```

See [MusicPlaylistItem](/nomercy-music-player/playlist-item) for field details.

### `MusicPlayerConfig<T>`

```ts
interface MusicPlayerConfig<
  T extends BasePlaylistItem = MusicPlaylistItem,
> extends BasePlayerConfig {
  backend?: AudioBackendKind;
  backendFactory?: AudioBackendFactory;
  crossfadeDefaults?: { duration: number; curve?: 'linear' | 'equal-power' };
  playlist?: T[] | string;
  trackEndingSoonThreshold?: number;
}
```

See [Configuration](/nomercy-music-player/configuration).

### `IMusicPlayer<T>`

```ts
interface IMusicPlayer<
  T extends BasePlaylistItem = MusicPlaylistItem,
> extends IPlayer<MusicEventMap> {
  backend(): IAudioBackend;
  backend(kind: AudioBackendKind): Promise<void>;
  crossfadeTo(track: T, opts?: CrossfadeOptions & ActionOptions): Promise<void>;
  isTransitioning(): boolean;
}
```

Recommended type for consumer-held player references.
See [Factory](/nomercy-music-player/api/factory).

### `MusicEventMap`

```ts
interface MusicEventMap extends BaseEventMap {
  current: { item: MusicPlaylistItem | undefined; index: number };
  'backend:changed': { kind: AudioBackendKind };
  repeat: { state: RepeatState };
  shuffle: { state: ShuffleState };
  trackEndingSoon: { remaining: number; currentTrack: MusicPlaylistItem };
  crossfadeStart: { from: MusicPlaylistItem | null; to: MusicPlaylistItem; duration: number };
  crossfadeComplete: { track: MusicPlaylistItem };
}
```

## Crossfade types

### `CrossfadeOptions`

```ts
interface CrossfadeOptions {
  duration: number;
  curve?: 'linear' | 'equal-power';
  startAt?: number;
}
```

### `AudioBackendKind`

```ts
type AudioBackendKind = 'audio-element' | 'webaudio';
```

### `AudioBackendFactory`

```ts
type AudioBackendFactory = (
  kind: AudioBackendKind,
  config: MusicPlayerConfig<BasePlaylistItem>,
) => IAudioBackend;
```

## Enums

### `PlayState`

```ts
enum PlayState {
  IDLE = 'idle',
  LOADING = 'loading',
  PLAYING = 'playing',
  PAUSED = 'paused',
  STOPPED = 'stopped',
  ERROR = 'error',
}
```

### `VolumeState`

```ts
enum VolumeState {
  UNMUTED = 'unmuted',
  MUTED = 'muted',
}
```

### `RepeatState`

```ts
enum RepeatState {
  OFF = 'off',
  ALL = 'all',
  ONE = 'one',
}
```

### `ShuffleState`

```ts
enum ShuffleState {
  OFF = 'off',
  ON = 'on',
}
```

## Backend types

### `BackendState`

```ts
type BackendState = 'idle' | 'loading' | 'ready' | 'playing' | 'paused' | 'error';
```

### `BackendLoaderState`

```ts
type BackendLoaderState = 'running' | 'paused';
```

### `BackendEvent`

```ts
type BackendEvent =
  | 'loadstart'
  | 'loadedmetadata'
  | 'canplay'
  | 'play'
  | 'playing'
  | 'pause'
  | 'ended'
  | 'timeupdate'
  | 'waiting'
  | 'stalled'
  | 'ratechange'
  | 'encrypted'
  | 'error'
  | 'backend:loading'
  | 'backend:loaded';
```

## Adapter types

| Type                            | Import path                                                                       |
| ------------------------------- | --------------------------------------------------------------------------------- |
| `ILyricSource`                  | `@nomercy-entertainment/nomercy-music-player/adapters/lyric-source`               |
| `IScrobbler`, `ScrobbleContext` | `@nomercy-entertainment/nomercy-music-player/adapters/scrobbler`                  |
| `INowPlayingArt`                | `@nomercy-entertainment/nomercy-music-player/adapters/now-playing-art`            |
| `IPlaylistGenerator`            | `@nomercy-entertainment/nomercy-music-player/adapters/playlist-generator`         |

## See also

- [MusicPlaylistItem field reference](/nomercy-music-player/playlist-item)
- [Events, full MusicEventMap with payloads](/nomercy-music-player/events)
- [IAudioBackend backend interface](/nomercy-music-player/backends/backend-interface)
