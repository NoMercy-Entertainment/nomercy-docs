---
title: 'EmbedPlugin'
description: "iframe embed and postMessage control — direct re-export of the kit's EmbedPlugin."
category: 'Plugins'
order: 11
draft: false
---

# EmbedPlugin

Iframe embed and `postMessage` control surface. This is a direct re-export of the kit's `EmbedPlugin` — no music-specific overrides.

```ts
import { EmbedPlugin } from '@nomercy-entertainment/nomercy-music-player/plugins';
```

---

## Registration

```ts
player.addPlugin(EmbedPlugin);
```

---

## See also

- [Player Kit — EmbedPlugin](/nomercy-player-kit/plugins-builtin) — full options, commands, and event reference
