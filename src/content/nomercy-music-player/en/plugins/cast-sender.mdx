---
title: 'CastSenderPlugin'
description: 'Chromecast sender for music — MusicTrackMediaMetadata, audio/mpeg default content type, and kit Cast lifecycle.'
draft: false
---

# CastSenderPlugin

Music Chromecast sender. A thin specialization of the kit's shared `CastSenderPlugin` that adapts music-specific fields (`name`, `artistTracks`, `albumTracks`, `cover`) to `MusicTrackMediaMetadata` and sets `'audio/mpeg'` as the default content type.

All Cast session lifecycle (SDK probe, session negotiation, `RemotePlayer` event mirroring, forward/rewind helpers, resume-on-disconnect) is handled by the kit base class.

```ts
import { CastSenderPlugin } from '@nomercy-entertainment/nomercy-music-player/plugins';
```

---

## Registration

```ts
const player = nmMPlayer('main').addPlugin(CastSenderPlugin).setup({ playlist: myTracks });
```

---

## Metadata mapping

| `MusicPlaylistItem` field               | `MusicTrackMediaMetadata` field |
| --------------------------------------- | ------------------------------- |
| `name`                                  | `title`                         |
| `artistTracks[].name` joined with `, `  | `artist`                        |
| `albumTracks[0].name`                   | `albumName`                     |
| `cover` (resolved via `resolveUrl`)     | `images[0].url`                 |

When `MusicTrackMediaMetadata` is not available from the Cast SDK, the plugin falls back to `GenericMediaMetadata`.

---

## Default content type

The plugin returns `'audio/mpeg'` as the default content type for music items. The Cast SDK uses this when building the `MediaInfo` object. Override per-item by including a `contentType` field on the playlist item.

---

## Cast session events

All Cast session events are namespaced under `plugin:cast-sender:*`. See the kit's `CastSenderPlugin` documentation for the full event list.

---

## Example

```ts
import nmMPlayer from '@nomercy-entertainment/nomercy-music-player';
import {
  CastSenderPlugin,
  AutoAdvancePlugin,
  MediaSessionPlugin,
} from '@nomercy-entertainment/nomercy-music-player/plugins';

const player = nmMPlayer('main')
  .addPlugin(CastSenderPlugin)
  .addPlugin(AutoAdvancePlugin)
  .addPlugin(MediaSessionPlugin)
  .setup({
    baseUrl: 'https://raw.githubusercontent.com/NoMercy-Entertainment/nomercy-media/master/Music/D/Derek%20Clegg/%5B2010%5D%20KJC',
    playlist: [
      {
        id: 'kjc-01',
        name: 'Thaw You Out',
        url: '/01%20Thaw%20You%20Out.mp3',
        cover: '/01%20Thaw%20You%20Out.jpg',
        artistTracks: [{ id: 1, name: 'Derek Clegg' }],
        albumTracks: [{ id: 1, name: 'KJC' }],
      },
    ],
  });

player.on('ready', () => {
  player.current(0, { autoplay: true });
});
```

---

## See also

- [MusicPlaylistItem](/nomercy-music-player/playlist-item) — `name`, `artistTracks`, `albumTracks`, `cover`
- [Player Kit — Cast](/nomercy-player-kit/cast) — shared Cast lifecycle, session events, forward/rewind helpers
