---
title: "Events"
description: "Complete event reference for NMMusicPlayer — MusicEventMap and all inherited BaseEventMap events, with payload types and usage notes."
category: "Events"
order: 27
draft: false
---

# Events

All events emitted by `NMMusicPlayer<T>`. Subscribe with `player.on(event, handler)`.

`MusicEventMap` extends `BaseEventMap` from the player kit. Music-specific events are listed first; inherited kit events follow.

---

## Music-specific events

### `current`

```ts
'current': { item: MusicPlaylistItem | undefined; index: number }
```

Fires when the current track changes. `item` is `undefined` when the queue is exhausted or cleared. `index` is `-1` in that case.

```ts
player.on('current', ({ item, index }) => {
  if (item) {
    document.title = item.name;
    updateAlbumArt(item.cover);
  }
});
```

### `trackEndingSoon`

```ts
'trackEndingSoon': { remaining: number; currentTrack: BasePlaylistItem }
```

Fires when the current track has `trackEndingSoonThreshold` seconds remaining (default: 10). `remaining` is the actual seconds left at the time of emission. `AutoAdvancePlugin` listens to this event for preload and crossfade cues.

### `crossfadeStart`

```ts
'crossfadeStart': { from: BasePlaylistItem | null; to: BasePlaylistItem; duration: number }
```

Fires when a crossfade begins. `from` is `null` when there is no current track. `duration` is the total fade duration **in milliseconds** (converted from the `duration` seconds value in `CrossfadeOptions` before emission).

### `crossfadeComplete`

```ts
'crossfadeComplete': { track: BasePlaylistItem }
```

Fires when the crossfade completes and the incoming track is now the primary.

### `repeat`

```ts
'repeat': { state: RepeatState }
```

Fires when `repeatState()` changes.

### `shuffle`

```ts
'shuffle': { state: ShuffleState }
```

Fires when `shuffleState()` changes.

### `eq:change`

```ts
'eq:change': { band: number; gain: number }
```

Fires when a single EQ band changes. `band` is the zero-based band index; `gain` is the new value in dB.

### `backend:changed`

```ts
'backend:changed': { kind: AudioBackendKind }
```

Fires when `backend(kind)` completes a backend swap. `kind` is `'audio-element'` or `'webaudio'`. Declared in `MusicEventMap` — fully typed, no cast required.

```ts
player.on('backend:changed', ({ kind }) => {
  console.log('Switched to', kind);
});
```

---

## Transport events (inherited)

| Event | Payload | When |
|-------|---------|------|
| `play` | `undefined` | Playback starts |
| `pause` | `undefined` | Playback pauses |
| `playing` | `undefined` | Media rendering after buffering resolves |
| `ended` | `undefined` | Current track finishes naturally |
| `waiting` | `undefined` | Buffer underrun |
| `canplay` | `undefined` | Enough data to begin playback |
| `stalled` | `undefined` | Data unavailable for unexpected period |

---

## Time events (inherited)

| Event | Payload | When |
|-------|---------|------|
| `time` | `{ time: number }` | Position updates (rAF-driven) |
| `duration` | `{ duration: number }` | Duration becomes known |

Use `time` to drive a real-time seekbar. Duration fires once after `loadedmetadata`.

---

## Queue events (inherited)

| Event | Payload | When |
|-------|---------|------|
| `queue` | `ReadonlyArray<T>` | Queue replaced |
| `queue:append` | `{ items: T[]; index: number }` | Items appended |
| `queue:prepend` | `{ items: T[]; index: number }` | Items prepended |
| `queue:remove` | `{ id: string \| number }` | Item removed |
| `queue:move` | `{ from: number; to: number }` | Item moved |
| `queue:clear` | `undefined` | Queue cleared |
| `queue:exhausted` | `undefined` | Queue played through with no repeat active |
| `backlog` | `ReadonlyArray<T>` | Backlog changes |

---

## Volume events (inherited from BaseEventMap)

`mute` and `volume` are inherited from `BaseEventMap` and are not re-declared in `MusicEventMap`.

| Event | Payload | When |
|-------|---------|------|
| `volume` | `{ level: number }` | Volume changes. `level` is on the 0–100 scale. |
| `mute` | `{ muted: boolean }` | Mute state changes |

---

## Phase events (inherited)

```ts
'phase': { from: PlayerPhase; to: PlayerPhase }
```

Fires on every player phase transition. Useful for orchestrating UI state (loading spinners, disabled controls).

PlayerPhase values: `'idle' | 'setup' | 'ready' | 'loading' | 'starting' | 'playing' | 'paused' | 'buffering' | 'seeking' | 'ended' | 'stopped' | 'disposing' | 'disposed'`

---

## Setup and lifecycle events (inherited)

| Event | Payload | When |
|-------|---------|------|
| `ready` | `undefined` | Setup complete, all plugins resolved |
| `firstFrame` | `undefined` | First audio frame decoded after load |
| `dispose` | `undefined` | Player teardown begins |

---

## Error events (inherited)

| Event | Payload | When |
|-------|---------|------|
| `error` | `PlayerErrorEvent` | Recoverable error |
| `fatal` | `PlayerErrorEvent` | Non-recoverable, playback stopped |
| `warning` | `PlayerErrorEvent` | Non-fatal degradation |

```ts
interface PlayerErrorEvent {
  code: string;       // e.g. 'plugin:lyrics/fetch-failed'
  message: string;
  severity: 'error' | 'fatal' | 'warning';
  cause?: unknown;
  context?: Record<string, unknown>;
}
```

---

## Auth events (inherited)

| Event | Payload | When |
|-------|---------|------|
| `auth:refreshed` | `{ token: string }` | Token refresh succeeded |
| `auth:failed` | `{ error: unknown }` | Token refresh failed |

---

## Subscribing and unsubscribing

```ts
const handler = ({ item }: { item: MusicPlaylistItem | undefined }) => {
  if (item) updateNowPlaying(item);
};

player.on('current', handler);   // subscribe
player.off('current', handler);  // unsubscribe
player.once('ready', () => {});  // one-shot
```

From inside a plugin, always use `this.on(...)` rather than `player.on(...)` — the plugin base class auto-disposes all its listeners when the plugin is removed.

---

## Plugin events

Plugin events are namespaced under `plugin:<id>:<event>`. Examples:

| Event | Payload |
|-------|---------|
| `plugin:lyrics:line` | `{ text: string }` |
| `plugin:lyrics:lineEnter` | `{ text: string }` |
| `plugin:lyrics:lineExit` | `{ text: string }` |
| `plugin:lyrics:loaded` | `undefined` |

Listen using the class form for full type inference:

```ts
import { LyricsPlugin } from '@nomercy-entertainment/nomercy-music-player/plugins';

player.on(LyricsPlugin, 'line', ({ text }) => {
  updateLyricsDisplay(text);
});
```

---

## See also

- [MusicPlaylistItem](/nomercy-music-player/playlist-item) — item shape in `current` payload
- [Crossfade](/nomercy-music-player/crossfade) — `crossfadeTo()`, crossfade event details
- [AutoAdvancePlugin](/nomercy-music-player/plugins/auto-advance) — listens to `trackEndingSoon` and `ended`
- [LyricsPlugin](/nomercy-music-player/plugins/lyrics) — `line`, `lineEnter`, `lineExit`, `loaded` events
