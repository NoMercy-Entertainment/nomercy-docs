---
title: 'Quick Start'
description: 'Get started writing against the nomercy-player-core directly, for library authors and plugin authors who need core-level access.'
---

# Player Core Quick Start

This page is for library authors and plugin developers who need to work against `nomercy-player-core` directly.
If you are building a video or music player, use the [Video Quick Start](/nomercy-video-player/quickstart) or [Music Quick Start](/nomercy-music-player/quickstart) instead, as both library packages include everything from the core.

## Install

```
npm install @nomercy-entertainment/nomercy-player-core@beta
```

## Writing a core-level plugin

A core-level plugin works with any player built on the player core, whether video, music, or a custom library you write.
Type the `P` generic broadly:

```ts
import { Plugin } from '@nomercy-entertainment/nomercy-player-core';
import type { IPlayer, BaseEventMap } from '@nomercy-entertainment/nomercy-player-core';

interface AnalyticsOptions {
  endpoint: string;
  sampleRateMs?: number;
}

interface AnalyticsEvents {
  'session:reported': { duration: number; rebufferRatio: number };
}

export class AnalyticsPlugin extends Plugin<
  IPlayer<BaseEventMap>,
  AnalyticsOptions,
  AnalyticsEvents
> {
  static readonly id = 'nomercy:analytics';
  static readonly version = '1.0.0';
  static readonly description = 'Reports session metrics to an analytics endpoint';

  use(): void {
    this.on('dispose', () => this.report());
  }

  dispose(): void {}

  private async report(): Promise<void> {
    const metrics = this.player.metrics();
    await this.fetch(this.opts.endpoint, {
      method: 'POST',
      responseType: 'json',
      body: JSON.stringify({
        duration: metrics.sessionDurationMs,
        rebufferRatio: metrics.rebufferRatio,
      }),
    });
    this.emit('session:reported', {
      duration: metrics.sessionDurationMs,
      rebufferRatio: metrics.rebufferRatio,
    });
  }
}
```

Register it on any player:

```ts
import { nmplayer } from '@nomercy-entertainment/nomercy-video-player';
import { nmMPlayer } from '@nomercy-entertainment/nomercy-music-player';

nmplayer('video')
  .addPlugin(AnalyticsPlugin, {
    endpoint: '/api/v1/analytics',
  })
  .setup({ ... });

nmMPlayer('music')
  .addPlugin(AnalyticsPlugin, {
    endpoint: '/api/v1/analytics',
  })
  .setup({ ... });
```

## Accessing adapter ports

Core adapters are available from the player core's subpath exports:

```ts
import {
  LocalStorageBackend,
  MemoryStorageBackend,
  DEFAULT_RETRY_POLICY,
  browserPlatform,
} from '@nomercy-entertainment/nomercy-player-core';
import type { IRetryPolicy } from '@nomercy-entertainment/nomercy-player-core';
```

See [Core: Adapters](/nomercy-player-core/adapters) for the full catalog.

## Testing core-level plugins

```ts
import { describePlugin } from '@nomercy-entertainment/nomercy-player-core/testing';
import { assertNoListenerLeak } from '@nomercy-entertainment/nomercy-player-core/testing';

describePlugin(AnalyticsPlugin, ({ player, plugin }) => {
  test('no listener leak', () => {
    assertNoListenerLeak(player, AnalyticsPlugin);
  });
});
```

The testing stub provides a minimal IPlayer implementation that satisfies the player core contracts without any browser APIs.

## What success looks like

A core-level plugin registers without errors, `use()` is called during `setup()`, and `assertNoListenerLeak` passes after dispose.
The plugin emits its typed events, and `player.on('plugin:analytics:session:reported', ...)` receives them.

## What to read next

- [Core: Adapters](/nomercy-player-core/adapters): the 28 infrastructure ports your plugin can swap
- [Core: Event System](/nomercy-player-core/event-system): typed events, cross-plugin listening
- [Core: Lifecycle](/nomercy-player-core/lifecycle): phase model and plugin ordering
- [Core: Testing](/nomercy-player-core/testing): full `describePlugin` and assertion API
- [Plugin Authoring](/nomercy-player-core/plugin-authoring): complete plugin contract reference
- [Advanced: Custom Plugin](/nomercy-player-core/advanced/custom-plugin): production-grade plugin patterns
