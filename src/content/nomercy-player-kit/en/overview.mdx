---
title: 'What is the Player Core'
description: 'The shared, headless engine the video and music players are built on.'
draft: false
---

{/* Source: packages/nomercy-player-kit/src/index.ts */}

# What is the NoMercy Player Core

The video player and the music player are mostly the same machine underneath.
A queue is a queue, auth is auth, and a plugin system does not care whether it is driving a film or an album.
`nomercy-player-core` is that shared machine pulled into one package, so the two players do not each carry their own copy.
It is headless and you never run it on its own; the library packages bring it along as a peer dependency.

## What's in it

- Transport and a queue engine (play, pause, seek, next, shuffle, backlog)
- The auth pipeline (bearer tokens, 401 refresh, 403 propagation)
- The plugin system and a typed event bus
- i18n and storage
- An adapter port for every cross-cutting concern, swappable without subclassing the player

A fix in any of these lands in both players at once.

## Where it sits

<LayerStack layers={[
  { n: 3, name: 'nomercy-video-player / nomercy-music-player', pkg: 'The library you install', knows: 'Video: HLS and subtitles. Music: Web Audio and crossfade.' },
  { n: 2, name: 'nomercy-player-core', pkg: 'You are here', knows: 'Queue, auth, plugins, event bus, i18n, storage, and adapter ports.' },
  { n: 1, name: 'Backend', pkg: 'Html5VideoBackend / WebAudioBackend', knows: 'hls.js, the native media element, or a custom backend.' },
]} />

The core knows nothing about NoMercy server APIs or your app; that lives in the layers above it.

## Next steps

- [Quick Start](/nomercy-player-core/quickstart): the smallest working setup
- [Architecture](/nomercy-player-core/architecture): the full five-layer model and how the player is composed
- [Adapter Ports](/nomercy-player-core/adapters): every port and its default
- [Plugins](/nomercy-player-core/plugins): the built-in plugins shipped with the core
