---
title: 'Persistence'
description: 'Choosing a storage backend, saving playback position, and restoring it on the next page load.'
---

# Persistence

Read this when you need to save and restore playback state across page loads — resume position, last-played track, volume level, subtitle preferences. The kit's storage adapter system handles all of this.

**Prerequisites:** [Kit — Adapters](/nomercy-player-kit/adapters) covers the three built-in storage backends.

---

## Storage backends

Three implementations ship with the kit:

```ts
import {
  LocalStorageBackend,
  MemoryStorageBackend,
  IndexedDBBackend,
} from '@nomercy-entertainment/nomercy-player-core';

// Default — persists across sessions, synchronous, shared across tabs:
player.setup({ storage: new LocalStorageBackend() });

// Ephemeral — ideal for tests or incognito; no persistence:
player.setup({ storage: new MemoryStorageBackend() });

// High-capacity — for large datasets (EQ preset libraries, subtitle cache):
player.setup({ storage: new IndexedDBBackend('my-player-db') });
```

The player and all built-in plugins write to storage under the player's namespace (`nmplayer-<id>-*`). You don't need to manage keys manually for built-in features.

---

## What the player persists automatically

With the default `LocalStorageBackend`, the player saves and restores:

- Volume level
- Mute state
- Subtitle language preference (the last selected language)
- Audio language preference
- EQ state (if `EqualizerPlugin` is registered)
- Subtitle style preferences (if `SubtitleOverlayPlugin` is registered)

Playback position is **not** saved automatically — that requires a consumer plugin because the player does not know where your server stores watch history.

---

## Saving and restoring playback position

Write a consumer plugin that saves position to your backend:

```ts
import { Plugin } from '@nomercy-entertainment/nomercy-player-core';
import type { NMVideoPlayer, VideoPlaylistItem } from '@nomercy-entertainment/nomercy-video-player';

interface ResumeOptions {
  saveIntervalMs?: number; // how often to save position. Default: 5000 (5s)
  minProgressSeconds?: number; // ignore saves before this point. Default: 5
}

export class ResumePlugin extends Plugin<NMVideoPlayer, ResumeOptions, {}> {
  static readonly id = 'myapp:resume';
  static readonly version = '1.0.0';
  static readonly description = 'Save and restore video playback position';

  private saveTimer: ReturnType<typeof setInterval> | null = null;

  use(): void {
    const intervalMs = this.opts.saveIntervalMs ?? 5000;
    const minSeconds = this.opts.minProgressSeconds ?? 5;

    // Save position periodically while playing:
    this.on('play', () => {
      this.saveTimer = this.interval(() => {
        const time = this.player.currentTime();
        const item = this.player.current();
        if (item && time > minSeconds) {
          this.savePosition(String(item.id), time);
        }
      }, intervalMs);
    });

    // Stop saving on pause/end:
    this.on('pause', () => this.clearSaveTimer());
    this.on('ended', () => this.clearSaveTimer());

    // Restore position when a new item becomes current:
    this.on('current', async ({ item }) => {
      if (!item) return;
      const saved = await this.loadPosition(String(item.id));
      if (saved && saved > 5) {
        // Seek to saved position after the player is ready for it
        this.player.once('canplay', () => this.player.currentTime(saved));
      }
    });
  }

  dispose(): void {
    this.clearSaveTimer();
  }

  private clearSaveTimer(): void {
    if (this.saveTimer !== null) {
      clearInterval(this.saveTimer);
      this.saveTimer = null;
    }
  }

  private async savePosition(itemId: string, time: number): Promise<void> {
    // Use this.fetch to get auth headers automatically:
    await this.fetch(`/api/progress/${itemId}`, {
      method: 'PUT',
      responseType: 'json',
      body: JSON.stringify({ position: time }),
      headers: { 'Content-Type': 'application/json' },
    });
  }

  private async loadPosition(itemId: string): Promise<number | null> {
    try {
      const data = await this.fetch<{ position: number }>(`/api/progress/${itemId}`, (r) =>
        r.json(),
      );
      return data.position ?? null;
    } catch {
      return null;
    }
  }
}
```

Register it:

```ts
player.addPlugin(ResumePlugin, {
  saveIntervalMs: 5000,
  minProgressSeconds: 10, // don't save if the user watched less than 10 seconds
});
```

---

## Local-only position saving (no server)

For a purely client-side implementation, use `this.storage` directly:

```ts
// Inside a plugin:
private savePosition(itemId: string, time: number): void {
  this.storage.set(`resume-${itemId}`, String(time));
}

private loadPosition(itemId: string): number | null {
  const saved = this.storage.get(`resume-${itemId}`);
  return saved !== null ? parseFloat(saved) : null;
}
```

`this.storage` reads from the same `IStorage` backend the player is configured with. Keys are automatically namespaced.

---

## Choosing the right storage backend for your context

| Scenario                | Backend                                                                                                                         |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| Standard web app        | `LocalStorageBackend` (default)                                                                                                 |
| SSR (Next.js, Nuxt)     | `MemoryStorageBackend` on server, `LocalStorageBackend` on client — see [Advanced: SSR](/nomercy-video-player/advanced/server-side-rendering) |
| Test suite              | `MemoryStorageBackend` — no cross-test contamination                                                                            |
| Large EQ preset library | `IndexedDBBackend`                                                                                                              |
| Sync across devices     | Custom backend calling your API                                                                                                 |

---

## IndexedDB backend

For large data (EQ snapshots, subtitle caches):

```ts
import { IndexedDBBackend } from '@nomercy-entertainment/nomercy-player-core';

player.setup({
  storage: new IndexedDBBackend('my-player-db'), // database name
});
```

`IndexedDBBackend` is async internally but the `IStorage` interface is synchronous. Writes are fire-and-forget; reads return the last committed value. For a fully async custom backend, see [Advanced: Custom Adapter](/nomercy-player-kit/advanced/custom-adapter).

---

## What to read next

- [Kit — Adapters](/nomercy-player-kit/adapters) — full `IStorage` interface and the backend catalog
- [Advanced: Custom Adapter](/nomercy-player-kit/advanced/custom-adapter) — writing an async IndexedDB-Worker backend
- [Recipes: Lyrics and Equalizer](/nomercy-music-player/recipes/lyrics-and-equalizer) — EQ state is persisted through this same storage system
